# 未定之旅

> 网页二次元回合制 RPG · 技术预览版 v0.16.0

## 技术栈

- **前端**：纯 HTML5 + Canvas + 原生 JavaScript（零框架依赖）
- **后端/数据库**：Supabase（免费版，PostgreSQL + RPC）
- **部署**：GitHub Pages（静态托管）+ PWA（可添加到主屏幕）
- **美术**：CSS/Canvas 占位符（后期替换为二次元立绘 + Q版战斗序列帧）
- **音频**：暂未实现

## 游戏系统

### 战斗系统
- 回合制，速度条驱动行动顺序
- 四人编队
- 大招在角色回合内释放（不可插队）
- 元素克制（金→木→土→水→火→金，克制 +20% / 被克制 -15%）
- 弱点系统待定

### 共鸣系统
- 同元素角色 ≥ 2 人 → 一阶共鸣（攻击力 +10%，元素力回复 +15%）
- 同元素角色 = 4 人 → 二阶共鸣（攻击力 +25%，元素力回复 +30%，共鸣技强化）
- 队伍中最多同时存在两种元素共鸣
- 角色倒下时共鸣动态重新计算

### 元素力系统
- 独立于大招能量的充能资源
- 普攻回复 15 点元素力（共鸣增益额外加成）
- 元素技消耗 25 点，共鸣技消耗 30/45 点（按阶数）

### 技能体系
- **普攻**：仅消耗本回合行动机会，回复元素力
- **元素技**：消耗一定元素力 + 本回合行动机会
- **共鸣技**：需激活共鸣，消耗元素力（一阶30/二阶45），二阶为强化版
- **大招**：消耗100能量（独立能量条）

### 剧情地图
- 节点图系统，多故事线分支
- 剧情对话 + 战斗关卡 + 选择分支
- 云端存档

### 角色与抽卡
- 常驻池 + 限定池
- SSR 1.6% / SR 13% / R 85.4%
- 90 抽保底 SSR，10 抽保底 SR

## 文件结构

```
├── index.html          # 入口页面
├── manifest.json       # PWA 清单（添加到主屏幕）
├── sw.js               # Service Worker（离线缓存，v0.12.0 增强分类缓存策略）
├── fix_supabase_rpc.sql # Supabase RPC 函数修复脚本
├── js/
│   ├── engine.js       # 核心引擎（场景管理、渲染、输入、资源加载、多种过渡特效）
│   ├── character-art.js # 角色美术系统（立绘加载+Q版序列帧+表情切换）← NEW
│   ├── battle.js       # 回合制战斗系统（含共鸣/元素力）
│   ├── battle-effects.js # 战斗粒子特效 + Sprite 特效
│   ├── battle-cutscene.js # 战斗演出系统（大招特写+技能演出+开场/胜利）← NEW
│   ├── battle-chain.js # 连击链/反击/元素连锁系统 ← NEW
│   ├── story.js        # 剧情/地图/关卡管理
│   ├── save.js         # Supabase 云端存档
│   ├── gacha.js        # 抽卡系统
│   ├── growth.js       # 角色养成系统（升级/突破/技能）
│   ├── stages.js       # 材料掉落关卡 / 每日挑战 / 体力系统 / 签到
│   ├── quests.js       # 每日委托/周常任务/活跃度系统 ← NEW
│   ├── characters.js   # 角色图鉴 & 编队管理
│   ├── ui.js           # 所有场景 UI 渲染
│   ├── ui-animations.js # UI 动画系统（HP条/伤害弹出/屏幕震动）
│   ├── achievements.js # 成就系统
│   ├── toast.js        # 全局 Toast 通知系统 ← NEW (v0.14.0)
│   ├── mail.js         # 邮件/收件箱系统 ← NEW (v0.14.0)
│   └── main.js         # 游戏入口 & 初始化
├── assets/             # 美术资源（占位，后期填充）
│   ├── characters/     # 角色立绘 & Q版序列帧
│   ├── maps/           # 地图背景
│   └── battle/         # 战斗特效
├── game_schema.sql     # Supabase 数据库建表脚本
└── README.md
```

## 开发进度

- [x] 核心引擎（场景管理、Canvas 渲染、输入系统、触摸手势）
- [x] 战斗系统（速度条、回合制、伤害计算、AI）
- [x] 共鸣系统（一阶/二阶共鸣、动态计算、增益效果）
- [x] 元素力系统（独立充能、普攻回复、技能消耗）
- [x] 元素克制（五行循环克制关系）
- [x] 共鸣技能（普通/强化，元素力消耗，共鸣状态检查）
- [x] 剧情系统（节点图、对话、分支选择）
- [x] 抽卡系统（概率池、保底机制）
- [x] Supabase 存档结构（SQL 脚本 + SDK 加载）
- [x] 登录/注册 UI 接入 SaveManager（含游客模式）
- [x] 角色图鉴场景
- [x] 编队管理场景（含共鸣预览）
- [x] 设置/关卡场景（占位）
- [x] 庸人工作室品牌标识
- [x] 角色养成系统（升级/突破/技能升级）
- [x] 抽卡结果入图鉴（新角色注册 + 重复角色命座）
- [x] 抽卡记录云端同步
- [x] 场景切换自动存档
- [x] 材料掉落关卡（突破材料 + 经验书）→ stages.js
- [x] 独立关卡模式（Farm 关卡 + 每日挑战）→ stages.js
- [x] 每日签到奖励系统 → stages.js
- [x] 体力系统（5分钟恢复1点，离线回复）→ stages.js
- [x] 战斗胜利奖励结算（掉落自动入背包）→ stages.js + ui.js
- [x] 五行元素粒子特效系统 → battle-effects.js
- [x] 战斗背景图集成（竞技场 / 幽暗森林）
- [x] 元素图标集成（战斗界面五行图标替换文字）
- [x] 抽卡背景图集成（五行召唤阵）
- [x] 粒子特效接入战斗动画（攻击/暴击/治愈/元素技能）
- [x] 设置场景完善（画质/帧率/存档/数据管理）
- [x] 废弃旧元素素材清理（冰/风/雷）
- [x] 邮件/收件箱系统（奖励邮件分发 + 一键领取 + 过期清理）→ mail.js (v0.14.0)
- [x] 全局 Toast 通知系统（操作反馈 + 队列管理 + Canvas 渲染）→ toast.js (v0.14.0)
- [x] 邮箱场景（列表 + 详情面板 + 附件领取）→ ui.js MailScene (v0.14.0)
- [x] 角色立绘接入剧情对话（说话人→角色映射 + 立绘渲染 + 淡入淡出过渡 + 表情推断）→ ui.js DialogueScene (v0.16.0)
- [ ] Q版战斗动画（Q版序列帧已接入战斗渲染 v0.15.0）
- [ ] 地图美术
- [ ] 剧情内容填充
- [ ] 音频资源制作与加载（BGM/音效文件，管理器已就绪）

## 在线体验

https://xvlain.github.io/game/

## 团队

- 游戏设计：三人团队
- 剧情 & 地图：元首
- 技术实现 & 美术：AI 辅助

## v0.16.0 更新日志

- **剧情对话立绘系统（ui.js DialogueScene）**：角色立绘正式接入剧情对话场景
  - `SPEAKER_CHAR_MAP` 说话人→角色ID映射表（织星/银发少女→zhixing，影/神秘旅者→ying）
  - `_renderPortraits()` 立绘渲染：根据说话人自动在画面左/右侧显示角色立绘
  - 说话人固定站位规则（织星左、影右、其他角色交替分配）
  - 非说话人立绘自动降低透明度（40%），旁白时所有角色降至 30%
  - `_updatePortraitTransition()` 淡入淡出过渡动画（250ms 平滑过渡）
  - `inferExpression()` 台词关键词自动推断表情（happy/sad/angry/surprise/default）
  - 说话人元素发光效果 + 底部光圈脉冲指示
  - 名字标签框内嵌角色头像图标
  - `_preloadNodePortraits()` 节点进入时预加载涉及角色的立绘 + 表情变体
  - 场景切换时自动清理立绘状态
- **全局错误处理与性能监控（engine.js）**：
  - 游戏循环 try-catch 保护，渲染异常不崩溃
  - 全局 `error` / `unhandledrejection` 捕获，最近 20 条错误记录
  - FPS 实时追踪（每秒刷新）+ 帧时间统计（平均/最大）
  - 调试信息覆盖层（左上角 FPS / 帧时间 / 场景名 / 错误数）
  - 设置→画面 新增"调试信息面板"开关
  - 严重错误自动弹出 Toast 提示
- **Service Worker v0.16.0-r2**：缓存版本更新，旧缓存自动清理
- 版本号统一升级至 v0.16.0

## v0.15.0 更新日志

- **Q版序列帧战斗渲染（ui.js BattleScene）**：战斗中角色从纯色占位面板升级为实际 Q版 Sprite 渲染
  - 6 个角色 × 5 种战斗动作（idle/attack/skill/ultimate/hit）= 102 帧 Q版序列帧全面接入
  - `characterArt.drawChibi()` 替代原有 `drawFramedPanel()` 占位渲染
  - 角色死亡时自动切换 defeat 状态（半透明 + 倒下动画）
  - 当前行动角色高亮标记（蓝色光圈）
- **Q版动画状态机**：新增 `_chibiStates` 追踪系统，由战斗事件自动驱动
  - 攻击者播放 attack/skill/ultimate 动画（根据技能类型自动选择）
  - 被攻击目标播放 hit 动画
  - 战斗胜利时全体存活角色播放 victory 动画
  - 战斗败北时全体角色播放 defeat 动画
  - 动画播放完毕后自动回归 idle 待机状态
- **Canvas 渲染优化**：坐标使用 `Math.floor()` 取整避免子像素抗锯齿开销
- **character-art.js 更新**：新增 defeat 动画定义（2帧，4fps，不循环）
- **预加载增强**：启动时预加载全部角色头像图标 + idle 帧 + 全动作帧
- **Service Worker v0.15.0**：缓存版本更新，旧缓存自动清理
- 版本号统一升级至 v0.15.0

## v0.14.0 更新日志

- **邮件/收件箱系统（mail.js）**：新增完整的邮件管理与奖励分发体系
  - 三种邮件类型：system（系统通知）/ reward（奖励邮件）/ event（活动邮件）
  - 附件支持：水晶、金币、道具，支持一键领取所有附件
  - 邮件状态：unread / read / claimed，自动过期清理（默认 30 天）
  - 邮箱容量上限 100 封，超出时自动删除最早的已读邮件
  - 邮件工厂模板：欢迎邮件、维护补偿、成就奖励、活动奖励、系统通知
  - 本地持久化 + 云端存档同步
- **全局 Toast 通知系统（toast.js）**：Canvas 渲染的非模态消息提示
  - 四种类型：info / success / warning / error，对应不同配色
  - 入场动画（滑入+淡入）+ 退场动画（淡出+上移）
  - 最多同时显示 3 条，超出进入队列自动排队
  - 支持 icon（emoji）+ 文本自动截断
  - 由引擎主循环驱动 update/render，渲染在画面最顶层
- **邮箱场景（ui.js MailScene）**：主菜单顶栏新增邮件入口
  - 左侧邮件列表（未读标记、类型图标、时间格式化、附件标记）
  - 右侧详情面板（标题、发件人、正文自动换行、附件展示、领取按钮）
  - 未读红点显示在主菜单邮件按钮上
  - 一键领取 + 滑动翻页支持
- **新玩家欢迎邮件**：首次进入主菜单自动发送含新手礼包的欢迎邮件
- **chibi-animator.js 帧索引修复**：帧编号从 1-based 改为 0-based，与素材命名规范 `idle_00.png` 一致
- **Service Worker v0.14.0**：缓存清单新增 `toast.js`、`mail.js`、`chibi-animator.js`
- 版本号统一升级至 v0.14.0

## v0.13.0 更新日志

- **每日委托系统（quests.js）**：新增完整的委托/周常任务体系
  - 每日委托：每天 04:00 自动重置，随机抽取 4 个任务
  - 周常任务：每周一 04:00 重置，7 个长期目标
  - 活跃度系统：完成委托累积活跃度，40/80/120/160 四档宝箱可领取
  - 任务类型覆盖：战斗、关卡、抽卡、养成、签到、连击链等 16 种事件
  - `QuestEventBridge` 事件桥接，自动追踪玩家行为
- **连击链系统（battle-chain.js）**：增强回合制战斗策略深度
  - 连击链（Chain）：同一目标连续受击伤害递增（每连 +5%，上限 30%）
  - 元素连锁（Elemental Chain）：连续同属性攻击触发 +15% 额外伤害
  - 破防系统（Break）：连续攻击累积破防值，满后眩晕目标 1 回合
  - 反击系统（Counter）：被攻击时概率反击，坦克 20%/输出 10%/治疗 5%
  - `BattleChainManager` 统一管理，与 `BattleEngine` 无缝集成
- **成就奖励领取**：所有成就解锁后可领取水晶/金币奖励
  - 28 个成就均配置独立奖励（水晶 20~200、金币 100~2000）
  - `claimReward()` 领取接口 + `getClaimableCount()` 未领取数量查询
  - Supabase `game_achievement_claims` 表支持云端持久化
- **Supabase 扩展**：新增 2 张数据表 + 4 个 RPC 函数
  - `game_quest_progress` 任务进度表（每日/每周重置周期隔离）
  - `game_achievement_claims` 成就领取记录表
  - `game_update_quest / game_claim_quest / game_claim_achievement / game_get_quests`
- **Service Worker v0.13.0**：离线体验优化
  - 离线访问返回专属 HTML 提示页（非空白 503）
  - 缓存清单新增 `battle-chain.js` 和 `quests.js`
  - 旧缓存自动清理机制保持
- **眩晕状态支持**：被破防的角色跳过回合，自动消耗眩晕层数
- 版本号统一升级至 v0.13.0

## v0.12.0 更新日志

- **角色美术系统（character-art.js）**：新增 `CharacterArtManager` 统一入口，包含：
  - `PortraitManager`：立绘加载器，支持表情变体（default/happy/sad/angry/surprise）、异步加载、fallback 占位
  - `ChibiAnimator`：Q版序列帧动画器，支持多动作（idle/attack/skill/hurt/death/victory）、帧率控制、循环/单次播放
  - `drawPortrait()` / `drawChibi()` / `drawIcon()` 三个渲染入口，自动在素材不存在时回退到 Canvas 占位符
- **战斗演出系统（battle-cutscene.js）**：新增 `BattleCutsceneManager` 演出控制器，包含：
  - 大招特写演出（2.5s 四阶段：蓄力→技能名展示→攻击粒子爆发→淡出，含角色立绘、元素粒子、屏幕震动）
  - 元素技/共鸣技演出（0.8-1.2s 快速演出，共鸣技额外双环特效）
  - 战斗开场演出（1.0s 幕帘+BATTLE 文字缩放）
  - 胜利演出（1.5s 金色粒子雨+VICTORY 文字）
  - 失败演出（1.0s 红色暗幕+DEFEAT 文字）
  - 支持跳过（skip）和队列播放
- **场景过渡增强**：SceneManager 支持三种过渡类型（fade 淡入淡出 / iris 光圈 / slide 滑动），通过 `{ transition: 'iris' }` 参数选择
- **引擎渲染增强**：GameEngine.render() 新增 `_cutsceneOverlay` 覆盖层渲染（演出系统在场景之上绘制）
- **Service Worker v0.12.0**：升级为分类缓存策略（静态资源 CacheFirst / 美术资源 StaleWhileRevalidate / API 不缓存 / CDN StaleWhileRevalidate），旧缓存自动清理
- **Supabase RPC 修复脚本**：新增 `fix_supabase_rpc.sql`，解决 PostgREST schema cache 导致函数找不到的问题
- 版本号统一升级至 v0.12.0

## v0.10.0 更新日志

- PWA 支持：新增 manifest.json + Service Worker，可添加到手机主屏幕，支持离线缓存
- 自动战斗：Farm 关卡新增"自动战斗"按钮，自动选择技能和目标（优先大招→战技→普攻，集火最低血量敌人）
- 剧情快进：已读对话自动进入快进模式，右上角快进按钮可手动切换，点击任意位置停止快进
- 玩家统计面板：新增"统计"场景，包含总览（冒险者信息/资源/收集率）、角色列表（按稀有度排序）、背包道具三个 Tab
- 主菜单布局升级为 4+4 网格
- 版本号统一升级至 v0.10.0

## v0.9.0 更新日志

- UI 边框素材集成：5 个九宫格边框（对话框/面板/按钮/HP条/能量条）预加载 + 渲染
- 新增 `drawNineSlice()` 九宫格绘制函数，保持边角不变形
- 新增 `drawFramedPanel()` / `drawFramedButton()` 通用绘制函数，素材不可用时自动回退
- HP 条渲染器接入战斗（平滑过渡 + 受击闪烁 + 低血量变色）
- 能量条渲染器接入战斗（五行元素颜色 + 满能量脉冲发光）
- 伤害数字弹出动画接入战斗行动（暴击金色、普通红色、治疗绿色）
- 屏幕震动效果（暴击触发，带衰减）
- 音频管理器升级为 Web Audio API（合成音效：点击/攻击/暴击/治愈/抽卡/升级/技能/大招）
- BGM 框架就绪（支持循环播放/淡入淡出，待音频文件）
- 引擎主循环驱动 UIAnimations.update(dt)
- 版本号统一升级至 v0.9.0

## v0.8.0 更新日志

- 集成道具图标系统（10 个道具图标：经验书 ×3、突破材料 ×5、金币、水晶）
- 预加载全部道具图标素材，UI 中自动替换 emoji 为实际图标
- 新增 `drawItemIcon()` 通用绘制函数，支持图标加载失败时回退到文字
- 战斗背景映射补全：虹彩圣域 → holy_sanctuary、混沌之主 → chaos_void
- 新增 `getBattleBgKey()` 函数，每日 Boss 按星期动态匹配战斗背景
- 抽卡结果展示优化：元素图标居中、SSR 卡片发光效果、NEW 标记升级为带背景色标签
- 养成场景升级页：经验书条目增加道具图标
- 主菜单资源栏：水晶 / 金币使用图标替代 emoji
- 版本号统一升级至 v0.8.0

## v0.7.0 更新日志

- 集成战斗背景图（暗黑奇幻竞技场 + 幽暗森林，按关卡类型自动匹配）
- 集成五行元素图标（金/木/水/火/土，战斗界面角色卡片替换文字为图标）
- 集成粒子特效系统到战斗场景（攻击/暴击/治愈/元素技能自动触发特效）
- 集成抽卡背景图（五行召唤阵）
- 集成工作室 Logo 到设置页面
- 设置场景完善：画质切换（自动/低/中/高）、帧率显示、自动存档开关、抽卡确认、清除本地存档（二次确认）、BGM/音效占位
- 清除废弃元素素材（ice/wind/lightning/wood_left/wood_right）
- 战斗背景半透明遮罩确保 UI 可读性
- 元素图标加载失败时自动回退到文字标签
- 版本号统一升级至 v0.7.0

## v0.6.0 更新日志

- 新增材料掉落关卡（经验书 × 3 档 + 突破材料 × 4 档）
- 新增每日挑战（一周七天元素 Boss 轮换）
- 新增体力系统（上限 120，每 5 分钟恢复 1 点，离线回复）
- 新增每日签到奖励（7 天循环，连续签到累计）
- 新增战斗胜利奖励结算（掉落自动计入背包，跳转结算场景）
- 新增五行元素粒子特效系统（金/木/水/火/土 × 攻击/技能/大招）
- 新增通用战斗特效（受击/暴击/治愈/共鸣触发/大招蓄力/大招释放）
- 新增战斗背景图（竞技场 arena_default.png + 森林 forest_dark.png）
- 新增抽卡背景图（五行召唤阵 gacha_summon.png）
- 版本号统一升级至 v0.6.0

## v0.5.0 更新日志

- 新增角色养成系统（升级/突破/技能升级）
- 新增经验道具系统（初/中/高级经验书）
- 突破系统（6 阶突破，每阶解锁更高等级上限）
- 技能升级系统（10 级上限，每级倍率+10%）
- 抽卡结果自动入图鉴（新角色注册 + 重复角色命座叠加）
- 抽卡记录云端同步（Supabase game_gacha_record）
- 场景切换自动存档（从养成/编队/抽卡/战斗/剧情地图离开时触发）
- 新增角色养成场景（GrowthScene）
- 主菜单新增"角色养成"入口（4+3 布局）
- 角色属性面板实时计算（等级×突破×技能等级）
- 立绘加载基础设施（assets/characters/portraits/<id>.png）
- 默认赠送 5 本初级经验书 + 2 本中级经验书
- 版本号统一升级至 v0.5.0

## v0.4.0 更新日志

- 新增登录/注册 HTML 覆盖层（支持移动端原生键盘输入）
- 新增游客模式入口（跳过认证，使用本地存档）
- 新增 AuthController：标题画面点击"开始/继续"触发登录流程
- 登录成功后按需加载云端存档（SaveManager.loadGame）
- 标题画面"继续游戏"仅在检测到本地存档时显示
- 版本号统一升级到 v0.4.0

## v0.3.0 更新日志

- 修复共鸣增益计算 bug（`energyRegen` 属性名错误）
- 新增共鸣技能按钮（战斗中根据共鸣状态动态显示）
- 新增角色图鉴场景（查看已拥有角色详情/技能）
- 新增编队管理场景（调整四人编队，含共鸣预览）
- 新增设置场景、关卡选择场景（占位）
- 引擎增强：资源加载器、触摸手势、头像绘制、滚动列表
- Supabase SDK 加载集成
- assets 目录规范完善
